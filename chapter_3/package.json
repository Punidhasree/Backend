{
  "name": "chapter_3",
  "version": "1.0.0",
  "description": "Dockerized full stack todo application using nodejs backend, postgresql database, prisma ORM ",
  "main": "db.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env --experimental-strip-types --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "prisma": "^6.18.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
